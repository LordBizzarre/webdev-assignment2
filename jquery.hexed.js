(function($) {

    $.fn.hexed = function(options) {
        // Establish default settings
        var 
        defaults = {
            difficulty: 5,
            turns: 10
        },
        settings = $.extend({}, defaults, options);
        


    }
}(jQuery));